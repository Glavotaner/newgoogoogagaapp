name: googoogagaapp
description: Da new googoogaga app.

publish_to: 'none'


environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  shared_preferences:
  firebase_core: ^1.7.0
  firebase_messaging: ^10.0.8
  flutter_local_notifications:
  provider:
  camera:

  http: ^0.13.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.9.2"

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/launcher.png"
